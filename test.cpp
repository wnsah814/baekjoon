#include <iostream>#include<cstdio>#include<algorithm>#include<vector>usingnamespacestd;typedefpair<int,int>pii;boolcomp(constpii&a,constpii&b){returna.first<b.first;}intmain(){vector<pii>arr;intn=10;for(inti=0;i<n;i++){intx,y;scanf("%d%d",&x,&y);arr.push_back(pii(x,y));/*arr.push_back(make_pair(a,b));이때C++11이상부터는arr.push_back({a,b});로도쓸수있다.*/}//lower_bound, upper_bound를쓰기위해sort를해준다.sort(arr.begin(),arr.end());for(inti=0;i<n;i++)cout<<"i::"<<i<<"x::"<<arr[i].first<<"y::"<<arr[i].second<<endl;//x가5이상인값이있는첫index를찾는과정이다.autolo_it=lower_bound(arr.begin(),arr.end(),pii(5,0),comp)-arr.begin();printf("lower_bound::%d\n",lo_it);//x가5초과인값이있는첫index를찾는과정이다.autoup_it=upper_bound(arr.begin(),arr.end(),pii(5,0),comp)-arr.begin();printf("upper_bound::%d\n",up_it);return0;}
